{
  "name": "secure-sync-extension",
  "version": "1.0.0",
  "description": "Cross-browser password and bookmark sync extension with end-to-end encryption",
  "scripts": {
    "build": "node build/build.js",
    "build:chrome": "node build/build.js chrome",
    "build:firefox": "node build/build.js firefox",
    "build:all": "node build/build.js all",
    "dev": "node build/build.js dev",
    "test": "jest",
    "test:integration": "jest --config jest.integration.config.js",
    "test:e2e": "playwright test",
    "lint": "eslint src/**/*.js"
  },
  "keywords": [
    "password-manager",
    "bookmark-sync",
    "cross-browser",
    "encryption",
    "chrome-extension",
    "firefox-addon"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "archiver": "^6.0.1",
    "eslint": "^8.56.0",
    "fs-extra": "^11.2.0",
    "jest": "^29.7.0",
    "webextension-polyfill": "^0.10.0"
  }
}
